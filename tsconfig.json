{
  // Este arquivo NÃO TEM compilerOptions.
  // Ele serve APENAS para dizer ao VS Code quais são os projetos.

  "files": [], // Diz ao TS que a raiz não tem arquivos .ts próprios
  "references": [
    // Liste TODOS os seus pacotes aqui
    { "path": "packages/api" },
    { "path": "packages/agente-tintas-ia" },
    { "path": "packages/database" }
  ]
}
