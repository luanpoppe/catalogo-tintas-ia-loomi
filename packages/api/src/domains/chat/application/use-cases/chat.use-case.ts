import { RequestChatDTO } from "../../infrastructure/http/dto/chat.dto";

export class ChatUseCase {
  constructor() {}

  async execute(body: RequestChatDTO) {
    const response = "";

    return { response };
  }
}
